import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pulhapanzap.appvt',
  appName: 'pulhapanzak-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
